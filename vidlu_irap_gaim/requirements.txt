numpy
opencv-python
pillow
tqdm
torch>=1.9.0
torchvision>=0.9.0
matplotlib
scikit-learn
# The dataset viewer, and `tools/vlm_chat_app.py`, which needs >= 1.43 for image
# attachments in `st.chat_input`.
streamlit>=1.43

# ViT backbones (SigLIP 2, SPAR, DINOv3) and their layer-wise LR decay parameter groups.
timm>=1.0.20
# Model downloads, including gated repositories (DINOv3) via HF_TOKEN.
huggingface_hub>=0.30

# VLM inference (for zero-shot evaluation)
# transformers >= 5.14 is required for Qwen3.5 (transformers.models.qwen3_5).
transformers>=5.14
qwen-vl-utils>=0.0.8
pyyaml>=6.0

# VLM fine-tuning (LoRA / QLoRA)
peft>=0.11
bitsandbytes>=0.43
accelerate>=0.30

# Optional, Qwen3.5 only: fused kernels for the Gated-DeltaNet layers.  Without
# them transformers falls back to a correct but slower pure-torch path.
# causal-conv1d
# flash-linear-attention
